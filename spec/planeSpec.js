describe("Plane", function() {
  var plane;

  beforeEach(function() {
    plane = new Plane();
  });

  it("should equal an instance of plane", function() {
    expect(plane).not.toBe(null);
    });
  });
